<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.task41">

    <application
        android:allowBackup="true"
        android:label="Task Manager"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Task41">


        <activity android:name="com.example.task41.AddTaskActivity" />
        <activity android:name="com.example.task41.TaskDetailsActivity" />

        <activity android:name="com.example.task41.MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
